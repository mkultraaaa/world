# msolo.me закрыт от индексации заголовком X-Robots-Tag: noindex (см. vercel.json).
# Краулинг намеренно разрешён: иначе поисковик не увидит noindex и страницы могут остаться в выдаче.
User-agent: *
Disallow:
